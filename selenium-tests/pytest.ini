[pytest]
markers =
    e2e: End-to-end tests that require a running MERN application

; Tell pytest where tests live and how to resolve imports
testpaths = tests
pythonpath = .

; Default options: verbose output + HTML report
addopts = -v --html=report.html --self-contained-html
